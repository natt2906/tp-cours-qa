<form method="post" action="{{ path('prefecture_delete', {'id': prefecture.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prefecture.id) }}">
    <button class="btn">Delete</button>
</form>
